#!/bin/bash
# Shim over linuxserver's wrapped-chrome.
# Right-click menu / desktop entries call this path; route them through our
# CDP-aware launcher so ENABLE_CDP keeps the dedicated profile + remote debugging.
exec /usr/local/bin/start-cdp-chrome.sh "$@"
